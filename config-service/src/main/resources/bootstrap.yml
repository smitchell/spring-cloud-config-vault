spring:
  application:
    name: config-service
  cloud:
    vault:
      host: localhost
      port: 8200
      scheme: https
      uri: http://localhost:8200
      connection-timeout: 5000
      read-timeout: 15000
      config:
        order: -10
      token: 00000000-0000-0000-0000-000000000000
#      ssl:
#        trust-store: classpath:/certs/keystore.jks
#        trust-store-password: changeit
